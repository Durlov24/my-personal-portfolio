/* // Submit form data to Google Sheets
$('#contact-form').submit(function (e) {
    e.preventDefault();
    var name = $('#name').val();
    var email = $('#email').val();
    var message = $('#message').val();
    // Code to send form data to Google Sheets
    // This requires setting up a server-side script to handle form submissions
});

// Download CV
$('#download-cv').click(function () {
    // Code to trigger CV download
}); */